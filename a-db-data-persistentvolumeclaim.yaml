apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  labels:
    io.kompose.service: a-db-data
  name: a-db-data
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 100Mi
